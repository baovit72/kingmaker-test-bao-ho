import { TransactionButton } from './TransactionButton';

export default {
  title: 'Header/Transaction Button',
  component: TransactionButton,
};

export const Default = () => <TransactionButton />;
